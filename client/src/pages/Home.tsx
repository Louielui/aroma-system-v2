/**
 * File intent: legacy file retained only for compatibility during Phase 1 architecture work.
 * Design reminder for this file: not used by current routing; keep minimal and presentation-neutral.
 */

export default function Home() {
  return <div>Home route is replaced by the Phase 1 application routing shell.</div>;
}
